# Power Grid Forecasting

A Python project for combining simulated grid-load, weather, and outage data to forecast electrical demand and estimate outage risk in Hudson County, New Jersey.

## Run

Install the required Python packages, then run:

```bash
python src/test.py
```

The raw weather and outage datasets are stored in `data/raw/`, and the main analysis notebook is `src/main.ipynb`.
